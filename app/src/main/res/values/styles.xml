<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 自定义仿IOS的ActionSheet底部Dialog的样式 ，有模糊效果 -->
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>

    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <!--启动页-->
    <style name="SplashAppTheme" parent="android:Theme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!--<item name="android:windowBackground">@drawable/layer_list</item>-->
    </style>

    <style name="MarginTopBottom">
        <item name="android:layout_marginTop">@dimen/edittext_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/edittext_margin_top</item>
    </style>

    <style name="EdtTopBottomLeft">
        <item name="android:layout_marginTop">@dimen/edittext_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/edittext_margin_top</item>
        <item name="android:paddingLeft">@dimen/edittext_padding_left</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/primary_blue</item>
        <item name="colorControlActivated">@color/primary_blue</item>
    </style>

    <style name="MyPadding">
        <item name="android:paddingTop">@dimen/edittext_margin_top</item>
        <item name="android:paddingBottom">@dimen/edittext_margin_top</item>
        <item name="android:paddingLeft">@dimen/edittext_padding_left</item>
        <item name="android:paddingRight">@dimen/edittext_padding_left</item>
    </style>
    <style name="PaddinLeftRight">
        <item name="android:paddingLeft">@dimen/edittext_padding_left</item>
        <item name="android:paddingRight">@dimen/edittext_padding_left</item>
    </style>

</resources>
